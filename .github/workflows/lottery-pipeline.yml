name: Eurojackpot Automated Pipeline

on:
  schedule:
    # Τρίτη & Παρασκευή 22:00 UTC = 01:00 Ελλάδας (καλοκαίρι) / 00:00 (χειμώνας)
    - cron: '0 22 * * 2,5'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Full Pipeline (Fetch + Predict + Email + Dashboard)
        run: python src/main.py
        env:
          PYTHONPATH: ${{ github.workspace }}
          LOTTERY_EMAIL_USER: ${{ secrets.LOTTERY_EMAIL_USER }}
          LOTTERY_EMAIL_PASS: ${{ secrets.LOTTERY_EMAIL_PASS }}
          LOTTERY_EMAIL_TO: ${{ secrets.LOTTERY_EMAIL_TO }}

      - name: Commit and Push Updated Database & Reports
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data/*.db reports/ || true
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update database and reports [skip ci]" && git push)

      - name: Run Tests
        run: python -m pytest tests/ -v || true
        env:
          PYTHONPATH: ${{ github.workspace }}

      - name: Upload Reports Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: lottery-reports
          path: |
            reports/
            data/
